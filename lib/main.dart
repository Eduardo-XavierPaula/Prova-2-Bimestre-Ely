import 'package:flutter/material.dart';
import 'package:persistencia/ui/listview_filme.dart';

void main() => runApp(
      MaterialApp(
        title: 'Carregando dados',
        home: ListViewFilme(),
      ),
    );

